<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:background="@android:color/black">

    <!-- UnifiedPlayerView owns surface-gesture arbitration. The activity remains responsible for
         the existing playback, brightness, volume, crop, audio and compatibility behavior. -->
    <com.example.cinestream.UnifiedPlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="true" />

    <!-- Top bar — Java sets paddingTop = statusBarHeight -->
    <LinearLayout
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="0dp"
        android:paddingEnd="8dp"
        android:paddingTop="0dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_back"
            android:contentDescription="Back"
            android:scaleType="fitCenter"
            android:padding="14dp" />

        <TextView
            android:id="@+id/tv_video_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:singleLine="true"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.01"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="8dp"
            android:shadowColor="#99000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="4" />

        <ImageButton
            android:id="@+id/btn_rotate"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="10dp"
            android:src="@drawable/ic_rotate"
            android:contentDescription="@string/rotate_description"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/btn_crop"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="10dp"
            android:src="@drawable/ic_crop"
            android:contentDescription="@string/crop_options"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/audio_track"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="10dp"
            android:src="@drawable/ic_audio_track"
            android:contentDescription="@string/audio_track_description"
            android:scaleType="fitCenter" />

    </LinearLayout>

    <!-- A small persistent lock affordance stays available even when Media3 controls auto-hide.
         It disappears while locked; deliberate bottom-left swipe is then the only app unlock path. -->
    <com.example.cinestream.ScreenLockButton
        android:id="@+id/btn_screen_lock"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="start|center_vertical"
        android:layout_marginStart="8dp"
        android:alpha="0.82"
        android:background="@drawable/overlay_background"
        android:contentDescription="@string/screen_lock_description"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_screen_lock" />

    <TextView
        android:id="@+id/gesture_feedback"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/overlay_background"
        android:fontFamily="sans-serif-medium"
        android:importantForAccessibility="no"
        android:paddingStart="18dp"
        android:paddingTop="11dp"
        android:paddingEnd="18dp"
        android:paddingBottom="11dp"
        android:textColor="#F5F7FB"
        android:textSize="15sp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/unlock_hint_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="58dp"
        android:background="@drawable/overlay_background"
        android:gravity="center_vertical"
        android:importantForAccessibility="no"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="10dp"
        android:paddingEnd="16dp"
        android:paddingBottom="10dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/unlock_hint"
            android:textColor="#E7EDF8"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- Volume overlay -->
    <LinearLayout
        android:id="@+id/overlay_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="16dp"
        android:background="@drawable/overlay_background"
        android:visibility="gone">

        <ImageView
            android:id="@+id/volume_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_volume" />

        <FrameLayout
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">

            <ProgressBar
                android:id="@+id/volume_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="6dp"
                android:max="100"
                android:maxHeight="6dp"
                android:minHeight="6dp"
                android:progress="50"
                android:progressDrawable="@drawable/progress_bar_purple" />

            <ProgressBar
                android:id="@+id/volume_boost_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="6dp"
                android:max="100"
                android:maxHeight="6dp"
                android:minHeight="6dp"
                android:progress="0"
                android:progressDrawable="@drawable/progress_bar_red_overlay" />

        </FrameLayout>

    </LinearLayout>

    <!-- Brightness overlay -->
    <LinearLayout
        android:id="@+id/brightness_overlay_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="16dp"
        android:background="@drawable/overlay_background"
        android:visibility="gone">

        <ImageView
            android:id="@+id/brightness_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_brightness" />

        <ProgressBar
            android:id="@+id/brightness_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="120dp"
            android:layout_height="6dp"
            android:layout_marginTop="8dp"
            android:max="100"
            android:maxHeight="6dp"
            android:minHeight="6dp"
            android:progress="50"
            android:progressDrawable="@drawable/progress_bar_orange" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
